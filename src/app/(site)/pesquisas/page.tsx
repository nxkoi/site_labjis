export default function Page() {
  return (
    <section className="space-y-4">
      <h1 className="text-2xl font-semibold">${p^}</h1>
      <p>Conteúdo de <code>/${p}</code> em construção.</p>
    </section>
  );
}
